package project.tokens;

// Usually an empty interface would be a bad practice
// I think this design pattern is appropriate this the context of creating a "language" of tokens to parse into
public interface IToken {}
